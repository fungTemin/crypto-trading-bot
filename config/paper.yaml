# Paper Trading Configuration
exchange:
  id: "paper"
  sandbox: false

risk:
  max_position_pct: 20       # more aggressive in paper
  max_daily_loss_pct: 10     # relaxed limits
  max_drawdown_pct: 30

logging:
  level: "DEBUG"
